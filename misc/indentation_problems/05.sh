if a; then
  doit
else
  els
  
# In line 4 one is to type "elsen" which is a custom defined function.
# "else" is a keyword defined in unindentThisLine property and
# "e" is electric key.
# It should be indented as it is initially:

if a; then
  doit;
else
  elsen
